# Automatically normalize line endings for all text files
* text=auto

# Force LF for shell scripts and SQL files
*.sql text eol=lf
*.sh text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf
